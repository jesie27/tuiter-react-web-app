/* eslint-env jquery */

const PostSummaryItem = (post) => {
    return(`
    <h1>test post summary item</h1>
    `);
}
export default PostSummaryItem;
//
// ${post.topic}
// ${post.userName}
// ${post.time}
// ${post.image}
// ${post.title}